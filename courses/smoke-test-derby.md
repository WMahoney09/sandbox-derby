# Smoke Test: Derby

## Objective

Verify that derby mode can run multiple sandboxes against the same course.

## Tasks

- [ ] Create a file called `proof.txt` containing the text "derby smoke test passed"
- [ ] Commit the file with message "smoke test complete"
